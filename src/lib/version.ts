// Build stamp. Bumped automatically by .claude/bump-version.mjs on each commit.
// No longer controls persistence — schema migrations live in the Zustand store's `migrate`.
export const APP_VERSION = '1.1.3';
